// Stable ProjectView navigation values. The string value is also the label
// rendered in the top-level menu.
export enum ProjectViewMenuStep {
    STATUS = "Status",
    REQUEST = "Request",
    SCRIPT = "Script",
    DOCUMENTS = "Documents"
}
